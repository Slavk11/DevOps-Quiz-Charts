replicaCount: 1

image:
  repository: ""
  tag: "latest"
  pullPolicy: IfNotPresent

containerPort: 8080

service:
  enabled: false
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: false
  className: nginx
  host: ""
  path: /
  pathType: Prefix
  tls:
    enabled: false
    secretName: ""

env: []

# Optional per-release ServiceAccount. When create=true, the chart
# creates a ServiceAccount named {{ release-fullname }} and binds it
# to the pod via serviceAccountName. When false, the pod uses the
# namespace's default ServiceAccount.
serviceAccount:
  create: false

healthcheck:
  enabled: true
  port: 8081
  liveness:
    path: /healthz
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 2
    failureThreshold: 3
  readiness:
    path: /readyz
    initialDelaySeconds: 5
    periodSeconds: 5
    timeoutSeconds: 2
    failureThreshold: 3

resources: {}